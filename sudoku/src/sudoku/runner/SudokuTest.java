package sudoku.runner;

class SudokuTest {
	public static void main(String[] args) {
		Sudoku sudoku = new Sudoku("example1.sud", 0.5);
		/* Functions:
		 * sudoku.Play(x, y, number); 
		 * sudoku.Random();
		 * sudoku.Undo();
		 * sudoku.Reset();
		 * ↓↓↓ Run a function here and the result will be displayed in the output image. */
		
		return;
		}
} 